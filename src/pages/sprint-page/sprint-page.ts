import { Control } from '../../components/Control';
import './sprintPage.scss';

export class SprintPage extends Control {
  constructor(parent: HTMLElement) {
    super(parent, 'main', 'main sprint-page', 'SPRINT-PAGE');
  }
}
