const baseURL = 'https://react-learnwords-example.herokuapp.com';

const words = `${baseURL}/words`;
const user = `${baseURL}/user`;
const MAX_PAGES = 30;
const MAX_GROUP = 6;
const TOKEN = 'RSLangMK-token';
const WORDS_ON_PAGE = 20;

export {
  baseURL, words, user, MAX_PAGES, MAX_GROUP, WORDS_ON_PAGE, TOKEN,
};
